import { ListenersDirective } from './listeners.directive';

describe('ListenersDirective', () => {
  it('should create an instance', () => {
    const directive = new ListenersDirective();
    expect(directive).toBeTruthy();
  });
});
